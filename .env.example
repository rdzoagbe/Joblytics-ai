# Copy this file to .env.local for local development
# In Railway: add this as an environment variable in your project settings

# The Anthropic key is SERVER-ONLY. Never prefix it with VITE_ — that would inline the
# secret into the client bundle and ship it to every browser. Set it server-side only:
ANTHROPIC_API_KEY=your_anthropic_api_key_here
